export enum UserRoles {
  ADMIN = 'admin',
  CARRIER = 'carrier',
  CUSTOMER = 'customer',
  DISTRIBUTOR = 'distributor',
  MESSENGER = 'messenger',
  PLANNER = 'planner',
  SCANNER = 'scanner',
}

export enum AuthorityType {
  AD = 'AD',
  B2C = 'B2C',
}
